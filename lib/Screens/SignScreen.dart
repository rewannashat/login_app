import 'package:flutter/material.dart';
import 'package:loginapp/Screens/sign.dart';

class SignScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: sign(),
      resizeToAvoidBottomPadding: false,
    );
  }
}
